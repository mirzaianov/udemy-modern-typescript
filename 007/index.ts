// let userName: string = 'Ivan';

// userName = '5';

const isBirsday: boolean = true;
const age: number = 40;
const userName: string = 'John';

if (isBirsday) {
  console.log(`Congrats ${userName}, age: ${age + 1}`);
}
